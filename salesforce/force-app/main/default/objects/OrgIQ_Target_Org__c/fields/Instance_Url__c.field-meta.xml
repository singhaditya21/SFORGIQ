<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Instance_Url__c</fullName>
    <description>My Domain / instance URL for this org, so a finding can be opened where it lives.</description>
    <inlineHelpText>My Domain / instance URL for this org, so a finding can be opened where it lives.</inlineHelpText>
    <label>Instance URL</label>

    <type>Url</type>
</CustomField>
